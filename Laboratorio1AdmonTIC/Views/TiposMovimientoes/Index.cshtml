@model IEnumerable<Laboratorio1AdmonTIC.Models.TiposMovimiento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoMovimiento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoMovimiento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoMovimientoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoMovimientoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoMovimientoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
