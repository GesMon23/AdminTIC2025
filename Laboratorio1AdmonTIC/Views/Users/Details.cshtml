@model Laboratorio1AdmonTIC.Models.TiposMovimiento

@{
    ViewData["Title"] = "Details";
}

<h1>Detalles</h1>

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Tipos de Movimientos</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.TipoMovimiento)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.TipoMovimiento)
            </dd>
        </dl>
        <div>
            <a class="btn btn-inverse-info btn-fw me-2" asp-action="Edit" asp-route-id="@Model?.TipoMovimientoId">Editar</a>
            <a class="btn btn-inverse-secondary btn-fw" asp-action="Index">Regresar</a>
        </div>
    </div>
</div>
