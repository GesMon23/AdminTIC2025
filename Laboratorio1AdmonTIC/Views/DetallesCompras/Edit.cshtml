@model Laboratorio1AdmonTIC.Models.DetallesCompras

@{
    ViewData["Title"] = "Edit";
}

<h1>Editando</h1>

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Detalles de Compra</h4>
        <hr />
        <div class="row">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="DetalleCompraId" />
                <div class="row">
                    <div class="form-group col-md-4">
                        <label asp-for="CompraId" class="control-label"></label>
                        <select style="height: 50px;" asp-for="CompraId" class="form-control" asp-items="ViewBag.Compras"></select>
                        <span asp-validation-for="CompraId" class="text-danger"></span>
                    </div>
                    <div class="form-group col-md-4">
                        <label asp-for="ProductoId" class="control-label"></label>
                        <select style="height: 50px;" asp-for="ProductoId" class="form-control" asp-items="ViewBag.Producto"></select>
                        <span asp-validation-for="ProductoId" class="text-danger"></span>
                    </div>
                    <div class="form-group col-md-4">
                        <label asp-for="Cantidad" class="control-label"></label>
                        <input type="number" asp-for="Cantidad" class="form-control" id="cantidad" />
                        <span asp-validation-for="Cantidad" class="text-danger"></span>
                    </div>
                </div>
                <div class="row">
                    <div class="form-group col-md-4">
                        <label asp-for="PrecioUnitario" class="control-label"></label>
                        <input type="number" asp-for="PrecioUnitario" class="form-control" id="precio" />
                        <span asp-validation-for="PrecioUnitario" class="text-danger"></span>
                    </div>
                    <div class="form-group col-md-4">
                        <label asp-for="Total" class="control-label"></label>
                        <input asp-for="Total" class="form-control" style="background-color: #2b2e4c; color: #f8f9fa;" id="total" readonly />
                        <span asp-validation-for="Total" class="text-danger"></span>
                    </div>
                </div>
                
                
                <div class="form-group">
                    <input type="submit" value="Guardar" class="btn btn-inverse-success btn-fw me-2" />
                    @* <a class="btn btn-inverse-secondary btn-fw" asp-action="Index">Regresar</a> *@
                </div>
            </form>
        </div>
    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        function calcularTotal() {
            const cantidad = parseFloat(document.getElementById("cantidad").value) || 0;
            const precio = parseFloat(document.getElementById("precio").value) || 0;
            const total = cantidad * precio;

            document.getElementById("total").value = total.toFixed(2); // Muestra con 2 decimales
        }

        document.getElementById("cantidad").addEventListener("input", calcularTotal);
        document.getElementById("precio").addEventListener("input", calcularTotal);
    </script>
}
